rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow list, update, delete, create: if false;
      allow get: if true;
    }
  }
}